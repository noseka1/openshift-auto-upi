helper:
  install_dir:                  '{{ ansible_env["HOME"] }}/openshift-auto-upi-install'
  install_cluster_dir:          '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}'
  install_bin_dir:              '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/bin'
  oc_exe:                       '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/bin/oc'
  install_exe:                  '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/bin/openshift-install'
  install_conf_dir:             '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/conf'
  mirror_registry_dir:          '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/mirror_registry'
  timestamp_dir:                '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/timestamps'
  timestamp_ignition_created:   '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/timestamps/ignition_created'
  timestamp_bootstrap_complete: '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/timestamps/bootstrap_complete'
  vsphere_dir:                  '{{ ansible_env["HOME"] }}/openshift-auto-upi-install/{{ openshift_install_config.name }}/vsphere'
  web_dir:                      '/var/www/html/openshift-auto-upi'
  web_cluster_dir:              '/var/www/html/openshift-auto-upi/{{ openshift_install_config.name }}'
  tftp_dir:                     '/var/lib/tftpboot/openshift-auto-upi'
  tftp_cluster_dir:             '/var/lib/tftpboot/openshift-auto-upi/{{ openshift_install_config.name }}'
